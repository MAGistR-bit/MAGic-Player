<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.projectMP3">

    <!-- Разрешение на чтение (и запись) данных внешнего хранилища -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/splash_screen_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/splash_screen_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MAGIC"
        tools:ignore="AllowBackup">
        <activity android:name="com.example.projectMP3.SplashScreenActivity"  android:noHistory="true" android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Прописываем классы в Manifest -->
        <activity android:name="com.example.projectMP3.Activity.AlbumDetailsActivity" />
        <activity android:name="com.example.projectMP3.Activity.PlayerActivity" />
        <activity android:name="com.example.projectMP3.Activity.MainActivity" />
        <activity android:name=".Activity.AboutAppActivity"/>
    </application>

</manifest>